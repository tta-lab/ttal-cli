Search the web using DuckDuckGo and return results.

Returns titles, URLs, and snippets for each result. Default: 10 results, max: 20.

Use this to find relevant URLs, then use read_url to read the content.
