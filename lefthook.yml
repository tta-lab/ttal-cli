pre-commit:
  parallel: true
  jobs:
    - name: fmt check
      glob: "*.go"
      run: make fmt && git diff --exit-code --quiet -- '*.go'

    - name: vet
      glob: "*.go"
      run: make vet

    - name: lint
      glob: "*.go"
      run: make lint
