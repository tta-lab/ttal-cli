# SCRATCH 2026-05-08 — fse.sub remaining NOT-RUN triage

Current report state after A10/A11/A12/gamma2d update:
- 54 PASS
- 0 FAIL
- 27 NOT-RUN
- 8 APPLE-LOCKED

## Downgrade Path

`DID_CHANGE_RENEWAL_PREF` downgrade is now split out as its own row:
`A10b DID_CHANGE_RENEWAL_PREF DOWNGRADE`.

Current DB state before Neil's new downgrade trigger:
- `subscription_events` had 2 `DID_CHANGE_RENEWAL_PREF` rows.
- Both were `subtype=UPGRADE`.
- No `DOWNGRADE` rows observed yet.

Live evidence after Neil triggered downgrade at 2026-05-08 17:36 CST:
- `subscription_events`: `DID_CHANGE_RENEWAL_PREF`, `subtype=DOWNGRADE`, `product_id=com.guion.flicknote.pro.yearly`, `created_at=2026-05-08 09:36:46.663396+00`, `signed_date=2026-05-08 09:36:46.138+00`, `transaction_id=2000001166708420`.
- `entitlements`: stayed `tier=pro`, `billing_cycle=yearly`, `expires_at=2026-05-08 10:34:29+00`, `auto_renew=true`.
- `entitlements.last_event_type` advanced to `DID_CHANGE_RENEWAL_PREF`.
- `entitlements.last_event_at` advanced to `2026-05-08 09:36:46.138+00`.
- Pod structured log: `webhook: DID_CHANGE_RENEWAL_PREF deferred-effect subtype=DOWNGRADE currentProduct=com.guion.flicknote.pro.yearly willRenewTo=com.guion.flicknote.plus.yearly`.
- Pod access log: `POST /webhook/apple` at `09:36:46` returned `200` in `28.75342ms`.

Interpretation:
- The deferred downgrade branch is live-verified for the pre-renewal state: it writes audit, advances HWM, keeps current entitlement tier/cycle.
- Still worth checking after the next sandbox renewal that the lower product is applied by `DID_RENEW`.
- Report updated: `A10b` added as PASS for the pre-renewal deferred-effect branch, with the post-renewal application check left in notes.

Expected live behavior for downgrade:
- `subscription_events` gets `event_type=DID_CHANGE_RENEWAL_PREF`, `subtype=DOWNGRADE`.
- `entitlements.tier` and `entitlements.billing_cycle` stay on the current product until the next renewal.
- `entitlements.last_event_type` and `last_event_at` advance to the `DID_CHANGE_RENEWAL_PREF` event.
- A later `DID_RENEW` should apply the lower product tier/cycle.

Why this matters:
- Upgrade is immediate; downgrade is deferred.
- The handler intentionally uses `UpsertEntitlementOnInitialPurchase` for `UPGRADE`, but `UpsertEntitlementAutoRenew` for `DOWNGRADE` and empty subtype.
- Live downgrade checks the no-tier-change branch, which A10's upgrade smoke did not cover.

## Best Next Live Checks

- `A10b` downgrade path: high value, likely live-testable via iOS sandbox subscription management while active.
- A tighter follow-up for downgrade renewal: after downgrade event, wait for next sandbox renewal and confirm the lower tier/cycle applies via `DID_RENEW`.

## Alpha7 Result

`alpha7` is now live-verified, including the post-fix validate audit append check.

Evidence:
- Current DB state before replay: active `pro/yearly`, `expires_at=2026-05-08 10:34:29+00`.
- Replayed stale VALIDATE JWS: transaction `2000001166661825`, product `com.guion.flicknote.plus.yearly`, `expires_at=2026-05-08 09:31:00+00`.
- HTTP 200 response stayed current paid: `tier=pro`, `billing_cycle=yearly`, `expires_at=2026-05-08T10:34:29Z`, `auto_renew=true`.
- Entitlement row unchanged before/after.
- Pod log: `skip entitlement upsert: transaction already expired ... handler=handleValidateSubscription`.

Post-PR #41 audit re-smoke:
- Current DB state before replay: active `pro/yearly`, `expires_at=2026-05-08 13:34:29+00`, `last_event_type=DID_RENEW`.
- Replayed the same stale VALIDATE transaction `2000001166661825`.
- HTTP 200 response stayed current paid: `tier=pro`, `billing_cycle=yearly`, `expires_at=2026-05-08T13:34:29Z`, `auto_renew=true`.
- Entitlement row unchanged before/after.
- Audit count for transaction `2000001166661825` advanced `1 -> 2`.

Resolved finding:
- Pre-fix cause: validate inserted `notification_uuid=""`, and `InsertSubscriptionEvent` uses `ON CONFLICT (notification_uuid) DO NOTHING`.
- Historical intended behavior: `VALIDATE` rows should write `notification_uuid IS NULL`, allowing request-granularity audit rows because Postgres unique indexes allow multiple NULLs.
- PR #41 restored SQL NULL behavior. The post-fix replay confirms audit append now works.

## Good Stub / Handler Tests

These are not good live integration targets because they require malformed, forged, or rare Apple payloads that sandbox does not expose.

- `E6` GRACE_PERIOD subtype sentinel. Notes already say unit-tested; likely can flip with fresh evidence or cite existing test.
- `E7` REVOKE audit-only.
- `E9` invalid AppAccountToken UUID.
- `E12` audit insert succeeds, entitlement upsert fails.
- `E13` unknown subscription productID.
- `E14` unknown neuron productID.
- `V4` empty `txn.AppAccountToken`.
- `V6` unsupported JWS type.
- `V8` Auto-Renewable with `txn.SignedDate=0`.
- `R3` GetEntitlement non-ErrNoRows DB error.
- `alpha2` decoded non-nil with nil TransactionInfo.
- `alpha3` `txn.SignedDate=0` COALESCE behavior.
- `alpha6` gwauth provides non-UUID userID.
- `alpha8` future-dated JWS.
- `alpha9` misconfigured catalog tier check violation.
- `alpha10` divergent user IDs with same consumable `(original_transaction_id, transaction_id)`.

## Better As SQL / Concurrency Tests

These are deterministic test-code targets, not good hand-driven live checks.

- `beta1` concurrent webhook + validate.
- `beta6` concurrent entitlement upserts.
- `gamma3a` stale `DID_RENEW` after fresh `EXPIRED`.
- `gamma3b` stale `SUBSCRIBED` after fresh `DID_RENEW`.

## Defer / Document Rather Than Chase Live

- `beta3` partial failure after audit insert: known recovery-design issue; likely belongs as DEFER until reconciliation is implemented or explicitly accepted.
- `beta5` request cancellation mid-handler: low value unless writing a context-cancel test.
- `I9` Apple retry semantics: sandbox does not prove production retry cadence; best evidence is docs plus production observation.
